language: android
jdk: oraclejdk8
android:
  components:
    - platform-tools
    - tools
    - build-tools-26.0.2
    - android-26
    - extra-android-support
    - extra-android-m2repository

before_install:
 - chmod +x gradlew

script:
    - cd gsyVideoPlayer
    - ../gradlew assembleRelease